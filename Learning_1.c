#include <stdio.h>

int main() {
    printf("Hello C Language : ) ");
    getchar();  // Use getchar() as a standard alternative to getch()
    return 0;   // Return 0 to indicate successful execution
}
